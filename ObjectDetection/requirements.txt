# Object Detection — Synthetic Blender Dataset + YOLOv8
# Install with: pip install -r requirements.txt

# Core training framework (includes torch, torchvision as transitive deps)
ultralytics>=8.0.0

# Image I/O and processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Numerical computing
numpy>=1.24.0

# Augmentation (used in post-processing / compositing scripts)
albumentations>=1.3.0

# Progress bars
tqdm>=4.65.0
